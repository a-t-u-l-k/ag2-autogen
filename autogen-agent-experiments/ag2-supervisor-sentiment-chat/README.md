# AG2 Supervisor Sentiment Chat

This demo routes chat replies based on sentiment and can switch from agent to supervisor behavior.

Files:
- `server.py`: backend service with sentiment-aware routing.
- `index.html`: lightweight UI for local testing.

Notes:
- External completion service values are placeholders in the checked-in code.
